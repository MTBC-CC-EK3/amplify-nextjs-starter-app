version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --frozen-lockfile
    build:
      commands:
        - yarn 
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
  cache:
    #paths:
     # - node_modules/**/*
